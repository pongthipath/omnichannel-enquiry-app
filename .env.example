# Expo inlines EXPO_PUBLIC_* at build time — never put secrets here
EXPO_PUBLIC_API_URL=http://localhost:4000/api/v1
EXPO_PUBLIC_SOCKET_URL=http://localhost:4000
# on a real phone use your computer's LAN IP instead of localhost, e.g. http://192.168.1.20:4000/api/v1
